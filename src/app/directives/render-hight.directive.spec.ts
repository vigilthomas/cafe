import { RenderHightDirective } from './render-hight.directive';

describe('RenderHightDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderHightDirective();
    expect(directive).toBeTruthy();
  });
});
